# Tool Catalog

> Auto-generated from `data/tool_catalog.yaml` — do not edit this file directly.
> To add or update a tool: edit `data/tool_catalog.yaml` then run `python scripts/gen_tool_catalog.py`.

## generic pack
*Available to: all archetypes*

| Tool | Description | Args |
|---|---|---|
| `get_search` | Search the web for recent news by keyword. | `query` (string) — Search keywords in English<br>`days` (int) *(optional)* — How many days back (default 7, max 30) |
| `get_memories` | Fetch the user's long-term memory topics (what the Duple remembers about them). | `duply_id` (string) — User ID |
| `get_knowledge` | Search ingested documents via vector similarity. Requires knowledge domain enabled. | `query` (string) — What to look up |
| `set_alert` | Set a price alert for a ticker on behalf of the user. Requires reach.alert enabled. | `ticker` (string) — Ticker symbol (e.g. AAPL)<br>`direction` (string) — "above" or "below"<br>`price` (number) — Target price |

## finance.generic pack
*Available to: all finance archetypes*

| Tool | Description | Args |
|---|---|---|
| `get_watchlist` | Fetch the user's saved watchlist with current price and analysis tags. | — |
| `update_watchlist` | Add or remove a ticker from the user's watchlist. | `action` (string) — "add" or "remove"<br>`ticker` (string) — Ticker symbol |

## finance.us pack
*Available to: US-market finance Duples*

| Tool | Description | Args |
|---|---|---|
| `get_stock_us` | Full price + technical + fundamental + news analysis for a US-listed ticker. | `ticker` (string) — US ticker symbol (e.g. NVDA) |
| `get_macro_us` | Macro overview (SPY/QQQ/GLD/rates) or macro news. | `mode` (string) *(optional)* — "overview" or "news" (default overview) |
| `get_sector_us` | Performance overview of all major US market sectors. | — |
| `get_theme_us` | Performance and holdings for one of 21 market themes (TECH, ENERGY, HEALTH, etc.). | `theme` (string) — Theme keyword (e.g. TECH, ENERGY) |
| `get_screener_us` | Screen US stocks by criteria (e.g. top gainers, high volume, RSI oversold). | `filter` (string) — Screen type (e.g. top_gainers, overbought) |
| `get_earnings_us` | Upcoming earnings dates, EPS estimates, and beat/miss history for a ticker. | `ticker` (string) *(optional)* — Ticker (omit for this week's calendar) |
| `get_economic_calendar_us` | High-impact US economic events for the current week (CPI, FOMC, NFP, etc.). | — |
