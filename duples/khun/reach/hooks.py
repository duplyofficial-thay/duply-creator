"""
hooks.py — Khun (finance archetype) reach triggers.

Stub — BigMoveEvent not yet wired. Wire by importing from Thay's hooks.py
or implementing a Khun-specific version once Khun's reach.alert is ready.
"""

EVENT_TRIGGERS = []
SCHEDULE_TRIGGERS = []


def generate_message(duply_id, fires, profile, system_lang):
    return None, None


def fallback_message(fires):
    return ["🔔"]
