# duples/{duple_id}/.env
# Copy this file to duples/{duple_id}/.env and fill in your credentials.
# This file is git-ignored — never commit the real .env.

POSTGRES_HOST=db.fpjevusrpausqunjhubk.supabase.co
POSTGRES_PORT=5432
POSTGRES_DB=postgres
POSTGRES_SCHEMA={duple_id}_ai
POSTGRES_ROLE={duple_id}_role
POSTGRES_PASSWORD=
