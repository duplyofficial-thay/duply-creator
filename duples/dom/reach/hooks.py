"""
hooks.py — Dom (lifestyle archetype) reach triggers.

Stub — no triggers active yet. Scaffold for future schedule triggers
(bedtime reminders, quest-due nudges) via reach_schedules once that
platform feature ships (see PATCH-NOTES.md v0.4.0).
"""

EVENT_TRIGGERS = []
SCHEDULE_TRIGGERS = []


def generate_message(duply_id, fires, profile, system_lang):
    return None, None


def fallback_message(fires):
    return ["🔔"]
